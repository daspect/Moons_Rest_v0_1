#pragma once
namespace ObjectIdentifier
{
	enum ID
	{
		Player,
		Background,
		Default,
	};
}

namespace EntityType
{
	enum ID
	{
		Background,
		Player,
		Movable,
		Immovable,
		Default,
	};
}

namespace Textures
{
	enum ID
	{
		Landscape,
		PlayerUp,
		PlayerDown,
		PlayerLeft,
		PlayerRight,
		Tree,
	};
}